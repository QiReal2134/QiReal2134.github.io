# 更新日志

## 2026-09-11

**修复**

- 滑动或方向键换屏后点导航失灵：锚点只在 hash 变化时才触发 `hashchange`，而滑动/方向键换屏不写 hash，
  于是「滑回首页后再点作品」时 hash 已经是 `#works`，浏览器什么都不做。现在点击时补判断一次，
  hash 与目标相同（浏览器不会导航）就直接切屏。

**变更**

- 静态资源版本戳 `?v=20260910` → `?v=20260911`，让老访客立刻拿到这次修好的 `script.js`。

## 2026-09-10

**导航与三屏**

- 导航项改为真锚点（`#home` / `#works` / `#about`）：链接可分享、支持前进后退、`index.html#works` 可直接定位。
- 非当前屏加 `inert`，Tab 焦点不会走进屏幕外的作品卡片；导航项标 `aria-current="page"`。
- 翻屏动画加锁（`transitionend` + 750ms 兜底），动画期间不接受新手势；窗口 resize 防抖 120ms。
- 手势改为按下即捕获指针，支持多指判定；纵向优先锁定，出现明显横向位移时重新接管为横向。
- 键盘 ←/→ 在输入控件内不再被接管。

**作品列表**

- 卡片改用 DOM API 构建（仓库名、描述走 `textContent`），不再拼接 `innerHTML`。
- 缓存 key 带上 `cacheVersion` + 账号 + 是否显示图片；新增 5 分钟新鲜期，减少匿名 GitHub API 的消耗。
- 背景图「没找到」改为 24 小时负缓存。此前把默认图当成命中存下，导致仓库后补图片也永远探测不到。
- 缓存形状校验：旧版本残留或被改坏的缓存直接丢弃，不会再渲染到一半崩掉。
- 3 秒兜底加载改为只在确实位于作品屏时触发，首页不再无条件请求 GitHub。

**作品详情**

- `?repo=` 参数加白名单校验，非法值不再拼进 API 路径。
- Release 名称、tag、日期改用 `textContent`；只有受控的 Markdown 输出进 `innerHTML`，并排除引号/尖括号/反斜杠。
- 仓库信息与 Release 列表改为并发请求；遇到配额耗尽（403/429 且剩余为 0）直接提示「约 N 分钟后重试」，不再做无意义的 1.5 秒重试。
- Release 按发布时间倒序排序，保证 `releases[0]` 永远是最新版。
- 兼容旧缓存的 `default_branch` 字段；`localStorage` 写入失败不再影响已经取回的数据渲染。
- 未指定作品或参数非法时，不再留下一个空的大图占位。

**雪花 / 样式**

- 雪花位图按 `devicePixelRatio` 缩放，高分屏上不再发虚、闪烁；片数随视口宽度自适应。
- 单一 `requestAnimationFrame` 句柄防重入；系统开启「减少动态效果」时整体禁用。
- 主题切换过渡改用 `:where(body, body *)`，去掉会把 `transform`/`opacity` 过渡一并清掉的 `!important` 简写，并单独还原翻屏轨道的节奏。
- 背景渐变与头像光晕改为双层交叉淡入（`radial-gradient` 无法插值，直接换变量会在切主题时闪一下）。
- 断点整理：纵向布局断点 680 → 880px，合并重复的 680px 段落，新增 380px 超窄屏；用 `overflow: clip` 规避意外的纵向滚动容器。
- 新增 `prefers-reduced-motion` 支持。

**其他**

- README 补充回源账号大小写、部署与缓存说明（版本戳与 `cacheVersion` 的用法）。
- `serve.js`：畸形百分号编码与 NUL 字节返回 400，越界判定改用 `path.relative`（字符串前缀比较会被同前缀的兄弟目录绕过），响应补 `Content-Length` 与 `Cache-Control: no-cache`，请求处理整体包一层 `try/catch`。
- 移除仓库里的构建产物 `master.tar.gz`。
